import SCENES from './scenes';
import PREFABS from './prefabs';
import EVENTS from './events';

const CONFIG = {
  GAME: {
    WIDTH: 800,
    HEIGHT: 800,
  },
  SCENES,
  PREFABS,
  EVENTS,
};

export default CONFIG;
